import { playlistSlice } from './slices/playlistSlice';
import { collectionSlice } from './slices/collectionSlice';

export const slice = [playlistSlice, collectionSlice];
export const sliceName = 'playlist-display';